import 'package:flutter/material.dart';

class TravelNotePage extends StatelessWidget {
  const TravelNotePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('用户游记'),
    );
  }
}